<template>
<!--    <div>
        <input type="text" placeholder="name" v-model="name"/>
        <div>
            <button
                @click="submitForm"
                @v-bind:disabled="name.length == 0" >
                Submit
            </button>
        </div>
    </div>
    -->
    <p> pico</p>
</template>
<script lang="ts">
/*import { onMounted, ref } from 'vue';
export default {
    setup(){
        const name = ref('')
        const el = ref()
        const submitForm = () => {
            console.log(`formulario subido por = ${name.value}`)
        }
        onMounted(() => {
            el.value.focus()

        })
        return {
            el,
            name,
            submitForm,
        }
    },
}
*/
</script>
<style scoped>
div{
    display: flex;
    flex-wrap: wrap;
    text-align: center;
}
</style>